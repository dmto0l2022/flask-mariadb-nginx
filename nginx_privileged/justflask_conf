server {
    listen       80;
    server_name  localhost;
    #location /app/ {
    #    proxy_pass   http://localhost:5000/;
    #}
    location / {
        proxy_pass http://localhost:5000/;
    }
}
